import 'package:flutter/material.dart';

class FarmLogisticsPage extends StatelessWidget {
  const FarmLogisticsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Farm Logistics'),
      ),
    );
  }
}
